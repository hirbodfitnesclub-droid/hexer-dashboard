# CURRENT_TASK.md — وضعیت جاری و تسک‌های سیستم (فاز نهایی مارکتینگ و اتریبیوشن)

این فایل حاوی گزارش بازخورد و وضعیت تسک‌های مارکتینگ و اتریبیوشن شامل تسک‌های ۱۳، ۱۴ و ۱۵ است.

---

## ۱. وضعیت جاری تسک‌ها [کاملاً موفق و تست شده]

- **📌 TASK 13 — طراحی و توسعه کامپوننت‌های بصری**: [✅ کاملاً موفق]
  - کامپوننت `FunnelChart.tsx` با Recharts و چیدمان لوکس تیره همراه با تولتیپ فارسی طراحی شد.
  - کامپوننت ماتریس حرارتی `RetentionMatrix.tsx` برای پیگیری میزان ماندگاری کاربران به شکل بصری و رنگ‌بندی حرارتی ساخته شد.
  - کامپوننت جدول ROI کانال‌ها `ChannelRoiTable.tsx` با پردازش بهینه‌ی مقادیر عددی و پول به تومان به همراه فرمت RTL و `toLocaleString('fa-IR')` پیاده‌سازی شد.
  - کامپوننت مودال ویرایش/ثبت کمپین `CampaignEditorModal.tsx` متصل به `ModalWrapper` با فیلدهای UTM، تاریخ و خودکارسازی ضرب هزینه جهت مبدل تومان به ریال دیتابیس ساخته شد.

- **📌 TASK 14 — توسعه داشبورد تحلیل مارکتینگ**: [✅ کاملاً موفق]
  - صفحه مجلل `MarketingDashboard.tsx` با کدهای کاملاً بهینه، بارگذاری موازی `Promise.all` از لایه `dataStore` و مدیریت فیلترهای لایو روی چارت‌های قیف تبدیل و توزیع زمان خرید ساخته شد.
  - چیدمان ۶ لایه شامل KPI کارت‌های ترافیک، قیف تبدیل، توزیع پیشرفته زمان خرید، هیت‌مپ ماندگاری، عملکرد سوددهی کانال‌ها و ماژول دقیق کاوش تک‌کمپینی (سازگار با سیستم گرید Tailwind) با زیبایی شگرف پیاده‌سازی و نهایی شد.

- **📌 TASK 15 — اتصال کامل ناوبری و تب مارکتینگ**: [✅ کاملاً موفق]
  - استور Zustand در `adminStore.ts` برای پشتیبانی ایمن از رشته‌ی `'marketing'` در نوع ActiveTab اصلاح شد.
  - منوی ناوبری کنار صفحه `AdminLayout.tsx` برای دسکتاپ و نمایشگرهای موبایل با کلید ناوبری جدید «تحلیل مارکتینگ» همراه با آیکون `TrendingUp` مجهز شد.
  - شرط رندر اختصاصی تب مارکتینگ در فایل روت `App.tsx` با ایمپورت ایمن متصل شد.

---

## ۲. درخت تمرکز (Focus Tree)

نمای فیزیکی فایل‌های درگیر در این فاز:

### فایل‌های جدید ساخته شده:
- `src/components/charts/FunnelChart.tsx` (چارت قیف تبدیل)
- `src/components/charts/RetentionMatrix.tsx` (جدول حرارتی ماندگاری کمپین‌ها)
- `src/components/marketing/ChannelRoiTable.tsx` (جدول عملکرد مالی کانال‌ها)
- `src/components/marketing/CampaignEditorModal.tsx` (مودال ویرایش و ایجاد کمپین)
- `src/pages/MarketingDashboard.tsx` (داشبورد جامع هوش بازاریابی)

### فایل‌های موجود ویرایش شده:
- `src/store/adminStore.ts` (ثبت و هم‌سوسازی استیت‌های فعال تب)
- `src/components/layout/AdminLayout.tsx` (افزودن منوهای دسکتاپ و کشویی موبایل)
- `src/App.tsx` (پورت روتر کانیشن کلاینت)

---

## ۳. بازخورد پایدار و وضعیت صحت بیلد

- تایید با لایوراست‌چین (RTL) به زبان فارسی: [✅ پاس شده]
- عدم وجود کدهای مسدودکننده یا Placeholderها در فرانت: [✅ پاس شده]
- بیلد سیستم و خروجی Production: [✅ موفق - بدون خطا]
- بهینه‌سازی بارگذاری کلاینت و جلوگیری از تداخل با RLS: [✅ پاس شده - صد درصد مبتنی بر لایه DTOها و Gateway]
